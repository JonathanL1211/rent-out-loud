<div class="container-fluid">
	<div class="row">
			<% @products.each do |product| %>
						<div class="col-sm-1 offset-sm-1 mt-4">
								<div class="card">
								  <%= cl_image_tag(product.product_image, class: "card-img-top img-fluid") %>
								  <hr/>
				  					<div class="card-body">
				  							<%= link_to product.name, product_path(product), class: "card-title h3" %>
										    <p class="card-text"><%= truncate(product.description, :length => 100, :separator => ' ') %></p>
										    <% if product.user_id != current_user.id %>
										    		<%= link_to 'Add to order', root_path, class: "btn btn-primary"%>
										    <% else %>
												    <%= link_to 'Edit' , edit_product_path(product), class: "btn btn-primary" %>
												    <%= link_to 'Delete' , product_path(product), method: :delete,
						      data: { confirm: 'Are you sure you want to delete this?' }, class: "btn btn-primary" %>
						     				<% end %>
										</div>
								</div>
						<br/>
						</div>
			<% end %>
	</div>
</div>